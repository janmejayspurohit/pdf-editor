// Stub — simulated config is only used in development/testing environments
// appVersion is kept for Gradle version sync: appVersion: '0.0.0'
export function getSimulatedAppConfig(): null {
    return null;
}
